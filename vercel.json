{
  "rewrites": [
    { "source": "/api/:path*",  "destination": "RENDER_BACKEND_URL/api/:path*" },
    { "source": "/auth/:path*", "destination": "RENDER_BACKEND_URL/auth/:path*" },
    { "source": "/health",      "destination": "RENDER_BACKEND_URL/health" }
  ]
}
